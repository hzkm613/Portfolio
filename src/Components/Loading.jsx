import React from "react";

const Loading = () => (
  <div className="absolute top-0 right-0 bg-black w-full h-full flex items-center justify-center z-50">
    <p className="text-white text-lg ml-4">Loading 3D model...</p>
  </div>
);

export default Loading;
